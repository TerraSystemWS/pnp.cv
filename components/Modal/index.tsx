// import { Button, Modal } from "flowbite-react";
// import React, { useState } from "react";

const Modali = ({ index, value }: any) => {
  // const onClick = (value: any, index: number) => {};
  // const onClose = () => {};
  // const open = () => {
  // 	return true;
  // };

  return (
    // <React.Fragment key={`A${index}`}>
    // 	{/* <Button onClick={onClick}>Toggle modal</Button> */}
    // 	<Modal show={open()} onClose={onClose}>
    // 		<Modal.Header>{value.titulo}</Modal.Header>
    // 		<Modal.Body>
    // 			<div className="space-y-6">
    // 				<p className="text-base leading-relaxed text-gray-500 dark:text-gray-400">
    // 					{value.descricao}
    // 				</p>
    // 			</div>
    // 		</Modal.Body>
    // 		<Modal.Footer>
    // 			<Button
    // 				color="gray"
    // 				onClick={() => onClick(value, index)}
    // 				className="bg-amarelo-escuro text-white hover:text-amarelo-ouro"
    // 			>
    // 				OK
    // 			</Button>
    // 		</Modal.Footer>
    // 	</Modal>
    // </React.Fragment>
    <p>modal</p>
  );
};

export default Modali;
